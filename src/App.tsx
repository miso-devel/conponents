import "./App.css";
import { useState } from "react";

function App() {
	const [_count, _setCount] = useState(0);

	return (
		<div className="App">
			作ったコンポーネントを置いておくためだけのプロジェクトです
		</div>
	);
}

export default App;
